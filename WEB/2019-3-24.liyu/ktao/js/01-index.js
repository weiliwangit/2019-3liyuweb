(function($){
	/*顶部导航逻辑开始*/
	var $dropdown = $('.dropdown');
	$dropdown.dropdown({
		
	});
	// $dropdown.on("dropdown-show dropdown-shown dropdown-hide dropdown-hidden",function(ev){
	// 	if(typeof "dropdown-show"){
	// 		var $elem=$(this);
	// 		var $layer=$elem.find(".dropdown-layer");
	// 		var url=$elem.data("load")
	// 	}
	// })
})(jQuery);